git checkout -b development sirve para cambiar a la rama development

git push -origin main 


git merge --no-ff dev p une las ramas seleccionadas a la dev

git checkout -b release/1.0.0

git tag -a 1.0.0 -m "version"

git checkout -b hotfix/change-message para hacer  un arreglo en caliente

